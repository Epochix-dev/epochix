version: "3.9"

services:
  server:
    build: .
    ports:
      - "7860:7860"
    environment:
      EPOCHIX_DB: /data/runs.db
      EPOCHIX_REDIS_URL: redis://redis:6379/0
    volumes:
      - epochix_data:/data
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: redis-server --save 60 1

  # Phase 5 (hosted) — not used in local mode
  postgres:
    image: postgres:16-alpine
    profiles: ["hosted"]
    environment:
      POSTGRES_DB: epochix
      POSTGRES_USER: epochix
      POSTGRES_PASSWORD: epochix_dev
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  # Optional: local LLM fallback
  ollama:
    image: ollama/ollama:latest
    profiles: ["llm"]
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  epochix_data:
  pg_data:
  ollama_data:
